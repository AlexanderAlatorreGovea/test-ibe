--middleware function
    termiates request response cycle or
    passes control to another middleware function
    middleware functions are called in order 
    and custom middleware functions should go on their own file
    
    - next() passes control to the next middleware function